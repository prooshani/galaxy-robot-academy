# Quick Quiz — Session 01

1. What does `print()` do?
2. Why does text need quotation marks?
3. Which line runs first?
4. Fix `Print("Hello")`.
5. Can code run and still have a logic problem?
6. Predict:

```python
print("A")
print("B")
print("A")
```

7. Name one debugging step.

**Passing score:** 5/7

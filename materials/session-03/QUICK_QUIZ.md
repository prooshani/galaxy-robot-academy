# Quick Quiz — Session 03

1. What does `input()` do?
2. What type does `input()` return by default?
3. Why use `int()`?
4. What does the `f` do in `f"Hello {name}"`?
5. Fix a program that asks for age and adds 1.
6. Identify an answer that was asked but never stored.
7. Explain one way input makes software interactive.

**Passing score:** 5/7

{
  "name": "galaxy-robot-academy",
  "private": true,
  "scripts": {
    "lint": "npx eslint apps/web/",
    "typecheck": "cd apps/web && npx tsc --noEmit",
    "build": "cd apps/web && npx next build",
    "dev": "cd apps/web && npx next dev"
  },
  "engines": {
    "node": ">=18"
  }
}
